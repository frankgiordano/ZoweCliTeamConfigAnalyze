package zowecli.globalteamconfig.sections;

public class ProfileProperties {

    // this will have many props most are optional, as such lets make this a builder pattern
    // but are the properties specify in the config json file known? if not and any value property can be
    // added then we will have a Map<String, String> properties variable

}
